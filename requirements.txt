# book toolchain (also needs Node.js >= 20, and XeLaTeX with xeCJK for the pdfs)
jupyter-book==2.1.6
ghp-import
# notebook kernel
ipykernel
jupyter-server
# packages used by the notebooks (pinned so printed output is reproducible)
matplotlib==3.10.3
numpy==2.3.1
pandas==2.3.1
scikit-learn==1.7.1
mplfonts
